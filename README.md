# kakeidoscope

A plugin for Kakoune which implements rainbow bracket highlighting.

## Installation

```bash
git clone https://git.sr.ht/~orchid/kakeidoscope
cd kakeidoscope/src
make install # with elevated privileges
cd ..
cp -r rc ~/.config/kak/autoload
```
